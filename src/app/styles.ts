import { styled } from '../../stitches.config'

export const Div = styled('div', {
  width: '100%',
  maxWidth: '1380px',
  marginInline: 'auto',
  paddingInline: '1.2rem',
  paddingBlock: '0.6rem',
  color: '$gray700',
})
